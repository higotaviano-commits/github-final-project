# Simple Interest Calculator

This project is a simple Bash script that calculates simple interest based on user input.

## Description
The Simple Interest Calculator asks the user for:
- Principal amount
- Interest rate
- Time period

It then calculates and displays the simple interest using the formula:

Simple Interest = (Principal × Rate × Time) / 100

## Usage
Run the script in a terminal:

bash simple-interest.sh
